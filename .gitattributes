# Одиторските файлове се пазят ПОБАЙТОВО. Подаденият в НАП файл е доказателство
# и краищата на редовете са част от него; core.autocrlf=true иначе би ги
# пренаписал при следващо изтегляне. `-text` изключва всякакво нормализиране.
nap-audit/*.xml -text
