# Pesenta.bg — Песен по поръчка

Онлайн платформа за поръчка на персонализирани песни. Статичен сайт (HTML/CSS/JS) — без билд стъпка, готов за GitHub Pages.

## Структура

```
index.html          — начална страница (hero, демо, поводи, стилове, цени, FAQ)
poruchka.html       — 3-стъпков формуляр за поръчка
plati.html          — страница за плащане (изпраща се на клиента след одобрение)
usloviya.html       — Общи условия (БАУФИЗИК ЕООД, отказ от право на връщане)
blagodarim.html     — резервна „благодарим“ страница
404.html            — страница за грешка
assets/css/style.css — целият дизайн (цветове в :root променливите)
assets/js/main.js   — аудио плейър, навигация, анимации
assets/js/order.js  — wizard, цени, промо кодове, изпращане
assets/js/payments.js — Stripe конфигурация (Payment Links, вкл./изкл.)
assets/audio/       — демо песните (MP3)
assets/img/         — SVG обложки, лого, OG изображение
```

## Къде се редактира какво

- **Цени и пакети** → `assets/js/order.js`, константата `PLANS` (+ съответните цени в `index.html`, секция `#ceni`). Текущи: Мини €9,90 / Соло €24,90 / Хит €39,90 / Спектакъл €69,90, експрес +€9,90.
- **Промо кодове** → `assets/js/order.js`, константата `PROMOS` (код → % отстъпка). Активни: `PESEN10` (−10%), `PRIYATELI20` (−20%).
- **Имейл за заявки** → `assets/js/order.js`, `FORM_ENDPOINT` и `ORDER_EMAIL`.
- **Демо песни** → добави MP3 в `assets/audio/`, копирай една `track-card` в `index.html`.

## Получаване на заявки (без бекенд)

Формата изпраща заявките през [FormSubmit](https://formsubmit.co) до `rusev.miro@gmail.com`.

⚠️ **Еднократна активация:** при първата изпратена заявка FormSubmit праща имейл за потвърждение — кликни линка в него и всички следващи заявки пристигат директно. Всяка заявка съдържа и готов **CLAUDE BRIEF** — копирай го в Claude за композиране на текста, после в Suno за музиката.

Ако FormSubmit е недостъпен, формата предлага на клиента да изпрати заявката през своя имейл клиент (mailto fallback) и пази черновата в localStorage.

## Публикуване (GitHub Pages + автоматичен deploy)

1. Създай публично repo `pesenta-bg` в GitHub (или `gh repo create pesenta-bg --public`).
2. ```
   git remote add origin https://github.com/<user>/pesenta-bg.git
   git push -u origin main
   ```
3. В **Settings → Pages** избери Source: **GitHub Actions**. Workflow-ът в `.github/workflows/deploy.yml` публикува автоматично при всеки push към `main`.
4. В **Settings → Pages → Custom domain** въведи `pesenta.bg` (файлът `CNAME` вече е в repo-то).

### DNS за pesenta.bg

| Тип | Хост | Стойност |
|-----|------|----------|
| A | @ | 185.199.108.153 |
| A | @ | 185.199.109.153 |
| A | @ | 185.199.110.153 |
| A | @ | 185.199.111.153 |
| CNAME | www | `<user>.github.io` |

## Активиране на Stripe (интегриран, но изключен)

Плащането се иска **след одобрение на демото**: от имейла на заявката копирай „Линк за плащане“
(`plati.html?order=…&plan=…`) и го изпрати на клиента, когато одобри песента.

1. Регистрирай Stripe акаунт за „БАУФИЗИК“ ЕООД и премини верификацията.
2. Създай продукт за всеки пакет и **Payment Link** за всяка комбинация
   (Мини/Соло/Хит/Спектакъл ± Експрес = 8 линка). Включи „Allow promotion codes“.
3. Пресъздай промо кодовете (PESEN10 −10%, PRIYATELI20 −20%) в Stripe → Coupons → Promotion codes.
4. Попълни URL-ите в `assets/js/payments.js` → `paymentLinks` и смени `enabled: true`.

### НАП чеклист преди първото картово плащане (алтернативен режим, Наредба Н-18)

- [ ] КЕП на фирмата (има ли валиден?).
- [ ] Регистрация на е-магазина в НАП (портал за е-услуги, преди старта на картовите плащания) — подават се данни за магазина, Stripe като доставчик и виртуалния ПОС; НАП издава уникален номер.
- [ ] Електронен документ за продажба (е-бележка) към всяко картово плащане — ще се добави при активацията.
- [ ] Месечен одиторски файл (XML, Приложение №38) — до 15-о число за предходния месец, през портала на НАП с КЕП.
- [ ] Потвърждение от счетоводителя (вкл. становище на НАП № 60-30-36/12.01.2026 за Stripe).

## GDPR

Сайтът е проектиран да не се нуждае от cookie банер: **няма аналитика, няма рекламни
бисквитки, няма външни заявки** (шрифтовете са self-hosted в `assets/fonts/` — без
Google Fonts CDN). Единственото съхранение е функционален localStorage, описан в
политиката.

- **Единственият източник на правните данни е `assets/js/legal-config.js`** —
  администратор, обработващи (FormSubmit, Gmail, GitHub Pages, Stripe), срокове,
  версия/дата. `poveritelnost.html` се попълва от него автоматично: нов доставчик или
  нов срок се добавя само там.
- При промяна: вдигни `privacy.version` и `privacy.updated` в конфига.
- ⚠️ Документите не следят закона сами — това е технически невъзможно. **Годишен
  чеклист** (или при нова услуга/доставчик): 1) списъкът с обработващи актуален ли е;
  2) добавена ли е нова обработка (аналитика? нюзлетър? — тогава трябва и cookie
  съгласие); 3) преглед на уведомленията на КЗЛД (cpdp.bg); 4) юрист при съмнение.
- Активиране на Stripe: платежните бисквитки живеят на страницата на Stripe
  (Payment Link), не на сайта — вече е описано в политиката.

## Музикален поздрав (начална страница)

Тих припев от „За теб, брате Влади“ тръгва ~3,5 сек след отваряне. Настройки в
`assets/js/main.js`, константата `INTRO`: `start` (секундата, от която почва
припевът — **нагласи на ухо!**), `duration`, `volume`, `delayMs`, `enabled: false`
за изключване. Браузърите блокират звук без взаимодействие — при отказ припевът
тръгва при първия клик/скрол. „✕“ на плаващата лента го спира завинаги за
устройството (localStorage `pesenta_intro_off`).

## Следващи стъпки

- Реални отзиви/истории в `index.html` (секция `#istorii`, маркирано с TODO).
- Google Search Console: добави домейна и изпрати `sitemap.xml`.
- Общите условия и Политиката за поверителност — прегледай с юрист преди пускане на плащанията.
